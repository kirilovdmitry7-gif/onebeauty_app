name: onebeauty_clean
description: OneBeauty — health, studio, store
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Совместимо и с локальной сборкой, и с Vercel
  intl: ">=0.19.0 <0.21.0"

  http: ^1.2.2
  shared_preferences: ^2.5.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # Для локального мок-сервера переводов; 1.4.1 совместим с collection 1.18.0
  shelf: ^1.4.1

flutter:
  uses-material-design: true
  generate: true
  # Если будут ассеты/шрифты — добавим здесь
  # assets:
  #   - assets/
